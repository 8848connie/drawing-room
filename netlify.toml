[build]
  functions = "api"
  publish = "public"

[functions]
  node_bundler = "esbuild"

# 只需要这一条核心规则，负责把前端的 /api/upload 映射到真正的函数上
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
